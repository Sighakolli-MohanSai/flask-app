version: "3.8"

services:
  flask-app:
    image: mohansai05/temp2:app

    ports:
      - "5000:5000"

    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure

    networks:
      - flask-network

networks:
  flask-network:
    driver: overlay
